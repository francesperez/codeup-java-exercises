package AbstractYoutube;

public abstract class Vehicle {
    abstract void go(); //we do not need to add a body to this method because they do not specify a body. It forces
    // us to implement the method in a child class.
}
